package dev.theagameplayer.puresuffering.world.entity;

public interface PSHyperCharge { //Add "PS" to avoid compatibility problems
    int psGetHyperCharge();
    
    void psSetHyperCharge(final int hyperChargeIn);
}
